{
  "name": "static-site",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "echo 'No build required'"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
